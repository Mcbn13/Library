@model Library.MVCWebUI.Models.CartSummaryViewModel

    <ul class="dropdown-menu">
        @foreach (var item in Model.Cart.CartLines)
        {
            <li>
                <a>@item.Book.Title <span class="badge">@item.Quantity</span></a>
            </li>
        }
        <li role="separator" class="dropdown-divider"></li>
        <li>
            <a><span class="glypicon glypicon-align-left glypicon-shopping-cart" aria-hidden="true"></span>@Model.Cart.Total</a>
        </li>
        <li role="separator" class="dropdown-divider"></li>
        <li><a asp-controller="Cart" asp-action="List">Go To Cart Details</a></li>
    </ul>